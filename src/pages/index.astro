---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Testimonials from '../components/Testimonials.astro';
import LogoGrid from '../components/LogoGrid.astro';
import Newsletter from '../components/Newsletter.astro';
import { getCollection } from 'astro:content';

// Fetch latest 3 blog posts
const allBlogPosts = await getCollection('blog');
const latestPosts = allBlogPosts
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 3);

// Fetch all products
const products = await getCollection('products');
---

<BaseLayout>
  <div class="bg-white">
    <Header />

    <!-- Hero Section -->
    <div class="relative">
      <div class="mx-auto max-w-7xl">
        <div class="relative z-10 pt-14 lg:w-full lg:max-w-2xl">
          <svg viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true" class="absolute inset-y-0 right-8 hidden h-full w-80 translate-x-1/2 transform fill-white lg:block">
            <polygon points="0,0 90,0 50,100 0,100" />
          </svg>

          <div class="relative px-6 py-32 sm:py-40 lg:px-8 lg:py-56 lg:pr-0">
            <div class="mx-auto max-w-2xl lg:mx-0 lg:max-w-xl">
              <div class="hidden sm:mb-10 sm:flex">
                <div class="relative rounded-full px-3 py-1 text-sm/6 text-gray-500 ring-1 ring-gray-900/10 hover:ring-gray-900/20">
                  Quick3 for WooCommerce er nå live! <a href="https://quick3-for-woocommerce.no/no/" class="font-semibold whitespace-nowrap text-indigo-600"><span aria-hidden="true" class="absolute inset-0"></span>Besøk nettsiden <span aria-hidden="true">&rarr;</span></a>
                </div>
              </div>
              <h1 class="text-5xl font-semibold tracking-tight text-pretty text-gray-900 sm:text-7xl">Vi puster liv i drømmer og produkter</h1>
              <p class="mt-8 text-lg font-medium text-pretty text-gray-500 sm:text-xl/8">Teknologi er vår lidenskap og vi elsker å bygge digitale produkter. Med kunder og for kunder.</p>
              <div class="mt-10 flex items-center gap-x-6">
                <a href="/produkter-og-kunder" class="rounded-md bg-indigo-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-xs hover:bg-indigo-500 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">Produkter og Kunder</a>
                <a href="/blogg" class="text-sm/6 font-semibold text-gray-900">Les bloggen <span aria-hidden="true">→</span></a>
              </div>
            </div>
          </div>
        </div>
      </div>
      <div class="bg-gray-50 lg:absolute lg:inset-y-0 lg:right-0 lg:w-1/2">
        <img src="https://images.unsplash.com/photo-1483389127117-b6a2102724ae?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1587&q=80" alt="" class="aspect-3/2 object-cover lg:aspect-auto lg:size-full" />
      </div>
    </div>

    <!-- Logo Grid -->
    <LogoGrid />

    <!-- Products Section -->
    <div class="bg-white py-24 sm:py-32">
      <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div class="mx-auto max-w-2xl lg:max-w-4xl">
          <h2 class="text-center text-pretty text-4xl font-semibold tracking-tight text-gray-900 sm:text-5xl">Produkter og kunder</h2>
          <p class="mt-2 text-center text-lg/8 text-gray-600">Vi bygger egne produkter, og hjelper våre kunder med å bygge sine</p>
          <div class="mt-16 space-y-20 lg:mt-20">
            {products.map((product) => (
              <article class="relative isolate flex flex-col gap-8 lg:flex-row">
                <div class="relative aspect-video sm:aspect-[2/1] lg:aspect-square lg:w-64 lg:shrink-0">
                  <img
                    alt=""
                    src={product.data.image}
                    class="absolute inset-0 size-full rounded-2xl bg-gray-50 object-cover"
                  />
                  <div class="absolute inset-0 rounded-2xl ring-1 ring-inset ring-gray-900/10" />
                </div>
                <div>
                  <div class="group relative max-w-xl">
                    <h3 class="mt-3 text-lg/6 font-semibold text-gray-900 group-hover:text-gray-600">
                      <a href={product.data.url} target="_blank" rel="noopener noreferrer">
                        <span class="absolute inset-0" />
                        {product.data.title}
                      </a>
                    </h3>
                    <p class="mt-5 text-sm/6 text-gray-600">{product.data.description}</p>
                  </div>
                  <div class="mt-6 flex border-t border-gray-900/5 pt-6">
                    <a
                      href={product.data.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="text-sm/6 font-semibold text-indigo-600 hover:text-indigo-500"
                    >
                      Besøk produkt <span aria-hidden="true">→</span>
                    </a>
                  </div>
                </div>
              </article>
            ))}
          </div>
        </div>
      </div>
    </div>

    <!-- Testimonials Section -->
    <Testimonials />

    <!-- Blog Preview Section -->
    <div class="bg-white py-24 sm:py-32">
      <div class="mx-auto max-w-7xl px-6 lg:px-8">
        <div class="mx-auto max-w-2xl text-center">
          <h2 class="text-4xl font-semibold tracking-tight text-balance text-gray-900 sm:text-5xl">Fra bloggen</h2>
          <p class="mt-2 text-lg/8 text-gray-600">Lær hvordan du kan vokse virksomheten din med våre ekspertråd.</p>
        </div>
        <div class="mx-auto mt-16 grid max-w-2xl grid-cols-1 gap-x-8 gap-y-20 lg:mx-0 lg:max-w-none lg:grid-cols-3">
          {latestPosts.map((post) => (
            <article class="flex flex-col items-start justify-between">
              <div class="relative w-full">
                <img src={post.data.image} alt="" class="aspect-video w-full rounded-2xl bg-gray-100 object-cover sm:aspect-2/1 lg:aspect-3/2" />
                <div class="absolute inset-0 rounded-2xl inset-ring inset-ring-gray-900/10"></div>
              </div>
              <div class="flex max-w-xl grow flex-col justify-between">
                <div class="mt-8 flex items-center gap-x-4 text-xs">
                  <time datetime={post.data.date.toISOString()} class="text-gray-500">
                    {post.data.date.toLocaleDateString('nb-NO', { year: 'numeric', month: 'short', day: 'numeric' })}
                  </time>
                  <a
                    href={post.data.category.href}
                    class="relative z-10 rounded-full bg-gray-50 px-3 py-1.5 font-medium text-gray-600 hover:bg-gray-100"
                  >
                    {post.data.category.title}
                  </a>
                </div>
                <div class="group relative grow">
                  <h3 class="mt-3 text-lg/6 font-semibold text-gray-900 group-hover:text-gray-600">
                    <a href={`/blogg/${post.slug}`}>
                      <span class="absolute inset-0"></span>
                      {post.data.title}
                    </a>
                  </h3>
                  <p class="mt-5 line-clamp-3 text-sm/6 text-gray-600">{post.data.description}</p>
                </div>
                <div class="relative mt-8 flex items-center gap-x-4 justify-self-end">
                  <img alt="" src={post.data.author.image} class="size-10 rounded-full bg-gray-100" />
                  <div class="text-sm/6">
                    <p class="font-semibold text-gray-900">
                      <a href={post.data.author.href}>
                        <span class="absolute inset-0"></span>
                        {post.data.author.name}
                      </a>
                    </p>
                    <p class="text-gray-600">{post.data.author.role}</p>
                  </div>
                </div>
              </div>
            </article>
          ))}
        </div>
        <div class="mt-16 text-center">
          <a href="/blogg" class="text-sm/6 font-semibold text-indigo-600 hover:text-indigo-500">
            Se alle innlegg <span aria-hidden="true">→</span>
          </a>
        </div>
      </div>
    </div>

    <!-- Newsletter Section -->
    <Newsletter />

    <Footer />
  </div>
</BaseLayout>
